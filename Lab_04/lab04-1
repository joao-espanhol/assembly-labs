main:
  # Le um numero do teclado e armazena em a0
  addi t0, zero, 4
  ecall

  # Salva o valor lido no registrado s0
  addi s0, zero, 0
  add s0, zero, a0

  # Imprima o valor de a0 na tela
  add a0, zero, s0
  addi t0, zero, 1
  ecall
  
ret
